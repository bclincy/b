<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{% block title %}{% if title %}{{ title }}{% else %}Brian Clincy a work in Progress{% endif %}{% endblock %}</title>
    <link href="{{ base_url() }}/css/bootstrap.min.css" rel="stylesheet"/>
    <link href="{{ base_url() }}/css/font-awesome.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ base_url() }}/css/default.css">
    <meta property="og:title" content="{% if title %}{{ title }}{% else %}Brian Clincy a work in Progress{% endif %}"/>
</head>
<body>
<header class="mb-3">
    {% include 'partials/nav.html.twig' %}
</header>